version: '3.4'

services:
  errorcenter:
    container_name: centralerroapi
    image: centralerroapi
    environment: 
      - PORT=5000
    ports: 
      - 5000:5000
    build:
      context: .
      dockerfile: Dockerfile